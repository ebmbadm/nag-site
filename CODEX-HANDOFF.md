# Codex handoff

## Последнее обновление

- Дата: 2026-08-05
- Компьютер: второй рабочий компьютер
- Репозиторий: `nag-site`
- Рабочая копия: `C:\NOVIK\nag-site`

## Активная задача

Продолжать работу с одной локальной копией на каждом компьютере и передавать
контекст только через Git и этот файл.

## Сделано

- Рабочая копия находится в `C:\NOVIK\nag-site`; съёмный диск `D:` не используется для работы с Git.
- Исправлены и опубликованы фото `NOVIK PA 602` и `NOVIK PA 1202`: TIFF-файлы с расширением `.jpg` перекодированы в JPEG.
- Исправление опубликовано в `main` коммитом `6b7e7e9`; CI/CD и проверка отображения на рабочем сайте прошли успешно.
- На главной странице заглушка в блоке «История NOVIK» заменена кнопкой «Читать историю» со ссылкой на `/istoriya`; удалено предложение «Сорок лет схемотехники…».
- Правка опубликована через PR №6 в `main` коммитом `d004e58`; CI/CD, развёртывание и прямая проверка сайта прошли успешно.
- Соглашение двух компьютеров находится в `docs/two-computer-work-agreement.md` в ветке `agent/site-ops-two-computer-agreement`.
- На втором компьютере база Obsidian перенесена в `C:\Users\Дмитрий\Desktop\База знаний`: проверены 3 229 файлов и 7 824 941 770 байт, папка `.obsidian` присутствует; Obsidian открыт с локальной копией.
- Съёмный диск `D:` очищен только от подтверждённых дубликатов: папки «Неразобранные фото усилителей» (1 380 файлов, побайтно совпадают с `C:\Users\Дмитрий\Documents\NOVIK_исходники`) и переносной копии базы Obsidian (3 229 файлов, проверка `robocopy` без ошибок и несовпадений).
- `D:\NOVIK26_SYNC` не изменялась. Запрос второму компьютеру о назначении и наличии полной локальной копии сохранён как `D:\02_ЗАПРОС_ВТОРОМУ_КОМПЬЮТЕРУ_NOVIK26_SYNC_2026-08-05.md`.

## Следующий шаг

Перед новой задачей выполнить `git fetch origin`, создать отдельную ветку от
актуального `origin/main` и работать только в ней. В конце сессии обновить
только этот файл, закоммитить его в `agent/codex-handoff` и отправить ветку в
`origin`.

До ответа второго компьютера не очищать `D:\NOVIK26_SYNC`.

## Git-состояние для проверки

- Перед началом: `git status --short --branch`
- Получить handoff: `git fetch origin agent/codex-handoff`
- Прочитать файл: `git show origin/agent/codex-handoff:CODEX-HANDOFF.md`

## Ограничения

Не добавлять `.env`, секреты, ключи, токены, cookie, архивы, медиа, сборки или `node_modules`.
